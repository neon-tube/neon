[package]
name = "neon-cli"
version.workspace = true
edition.workspace = true

[dependencies]
color-eyre = { version = "0.6", default-features = false, features = ["track-caller"] }
eyre = "0.6.12"
neon-compiler = { path = "../compiler" }
neon-runtime = { path = "../runtime" }
